import React from 'react';
import { Range } from '@/components/Range';

const Exercise1Page = () => {
  return (
    <div>
      <Range min={20} max={80} />
    </div>
  );
};

export default Exercise1Page;
